# Set a value for password and email
TQ_PASSWORD=
TQ_EMAIL=changeMe@junk.com
TQ_EMAIL2=
# Required for `yarn test:integration`. Never point this at production: the
# tests create projects and users, and register a site if login fails.
TQ_HOST=https://stage-api.testquality.net
